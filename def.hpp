#include <iostream>
#include <vector>
#include <string>

#define REP(i,n) for(int i=0;i<(int)(n); ++i)

using namespace std;

template<typename T>
using line = vector<T>;
template<typename T>
using table = vector<line<T>>;
