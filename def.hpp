#include <algorithm>
#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <tuple>
#include <queue>

#define REP(i,n) for(int i=0;i<(int)(n); ++i)

using namespace std;

using line = vector<bool>;
using table = vector<line>;
